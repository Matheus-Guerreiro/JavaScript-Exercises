const number = 5

const randomNumber = (Math.floor(Math.random() * (10 - 1) + 1));

if (number === randomNumber) {
    
     console.log("Você é o ganhador, parabéns!!!");

}else{

    console.log("Você não é o ganhador tente novamente");
    
}